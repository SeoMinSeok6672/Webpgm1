import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";  // App 컴포넌트 import

const root = document.getElementById("root");
ReactDOM.createRoot(root).render(<App />);  {/* App 컴포넌트를 렌더링 */}
